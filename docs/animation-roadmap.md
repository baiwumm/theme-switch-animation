# 动画类型候选池与执行顺序

> 这份文档只管"下一个做什么"。每开工一个就把那一行的状态改掉；验证通过后落到 `requirements.md` 的修订记录里，本文件随之精简。
>
> 最后更新：2026-09-23 —— 16 种已落地（v1.7 `RIPPLE`、v1.8 `CLOCK_SWEEP` / `FAN`、v1.9 `CURTAIN`），下一轮从 P0-2 `SPIRAL` 开始。

---

## 1. 三条不可谈判的约束

任何候选先过这三关，过不了直接进 §4 不做清单：

1. **只用 mask 动画。** Safari 忽略 view-transition 伪元素上的 `clip-path` 与 WAAPI，所以不碰 transform / clip-path / CSS filter 动画。唯一例外是 `CIRCLE_BLUR`——它的高斯模糊是 `feGaussianBlur` 烘焙进 SVG 蒙版本身，不是 CSS filter。
2. **末帧必须完全覆盖。** 注入的样式在转场结束后还要延迟 `duration` 才清理，蒙版期间持续生效（`fill: both`）。任何"末帧仍留镂空或半透明"的方案都不可接受。这条已经否掉了 FAN 的软尾（改成硬边）和 DISSOLVE 的朴素做法。
3. **转场期只有伪元素能动。** 真实页面被冻结，JS 驱动的装饰动画不会播放；转场前插进 DOM 的东西会被拍进旧截图。所以一切效果必须是纯 CSS 蒙版几何。

---

## 2. 现有 16 种覆盖了哪些维度

| 维度 | 已覆盖 | 缺口 |
| --- | --- | --- |
| 起点 | 触发点向外（CIRCLE 家族 7 + RIPPLE + CLOCK_SWEEP + FAN）、全屏轴推进（BLINDS / SCAN / QR_GRID）、中线对称推开（CURTAIN）、旧层收起（CIRCLE_REVERT） | **多中心 / 错时**（P1-3） |
| 前缘形态 | 硬边、软边、高斯模糊边、环带回摆、旋转射线 | 碎粒边（DISSOLVE，被约束 2 卡住） |
| 推进量 | 半径（`<length>`）、轴平移（`<length>`）、逐格生长（`<length>`）、角度（`<angle>`） | **半径 × 角度联动**（P0-2） |
| 轮廓来源 | 内置 SVG 多边形 | **用户自定义轮廓**（P2-2，要先定消毒规则） |
| 方向 | 四向（`direction`）、去/回（CIRCLE_REVERT） | 通用反向（P2-1，与 CIRCLE_REVERT 有语义撞车） |

**结论**：形状族（7 个）已经饱和，再加多边形只是同质堆叠。真正稀缺的是**多中心**、**半径×角度联动**、**自定义轮廓**这三条。

---

## 3. 执行顺序

状态取值：`待做` / `进行中` / `已落地` / `已撤回`。

### P0-1 `CURTAIN` 双开门

| 项 | 内容 |
| --- | --- |
| 观感 | 新主题从屏幕中线向两侧对称揭开，像拉开幕布 |
| 机制增量 | **无** |
| 实现要点 | `masks.ts` 里 `qrCenterGradient` 已经是"从中心向两侧对称生长"的写法（QR_GRID 的垂直轴层在用），把它从格子逻辑里拎出来当独立蒙版即可；蒙版盒子静止、复用 `REVEAL_VAR` |
| 参数面 | 不加。`direction` 对它静默无效（与 RIPPLE / CLOCK_SWEEP 一致） |
| 待验点 | **中缝接缝已实测排除**（headless Chrome 冻结进度截图）：r=640 时实心段精确落在 320→960、中缝无可见接缝、两侧 24px 软边对称；末帧整平面实心。起始帧中缝透出一道约 ±24px 的光，是加分观感而非缺陷。**仍未验**：分数缩放 dpr 下中缝与软边会不会出现 1px 级亮暗线 |
| 状态 | **已落地（v1.9）**：core + 画廊第 16 张卡 + 10 例单测 + 探针实测 + 全部门面同步。唯一遗留是分数缩放 dpr 一档待真机看 |

**为什么排第一**：最不可能失败，先用它把"做一个 → 量一次 → 验一次 → 提交"的节奏跑顺。

### P0-2 `SPIRAL` 螺旋扫开

| 项 | 内容 |
| --- | --- |
| 观感 | 射线一边绕轴心转、一边向外长，新主题呈螺旋铺满 |
| 机制增量 | 一个 keyframes 里同时动两个注册属性（`SWEEP_VAR` + `REVEAL_VAR`）；两正交蒙版层求交 |
| 实现要点 | "角度已扫过 **∩** 半径已长出"= conic 层与 radial 层用 `mask-composite: intersect`——这正是 QR_GRID 已跑通的路子（含 `@supports (mask-composite: intersect)` 门控）。降级基线取半径层（观感退化成 CIRCLE） |
| 参数面 | 建议不加。若要"螺距"就得第二个参数，先不做 |
| 待验点 | ① 两个 `@property` 在同一 keyframes 里是否都逐帧插值（单个已实测成立，两个未验）；② 末帧两层是否都盖满；③ 不支持 intersect 的引擎降级观感 |
| 状态 | 待做 |

**辨识度最高的一个**，机制全是已验证两块的组合，但比 P0-1 多一层不确定性。

### P1-3 `SEEDS` 多点错时扩散

| 项 | 内容 |
| --- | --- |
| 观感 | 屏幕上 3~4 处先后各自长出圆斑，陆续拼满，像墨点渗透 |
| 机制增量 | **无**（多层 radial + 错时） |
| 实现要点 | 多个 `radial-gradient` 图层，各层进度写成 `calc(var(--theme-switch-reveal) - 偏移px)` 错开，层间默认 `mask-composite: add` 求并集。P0 本轮已实测 `calc(var(--sweep) - 12deg)` 被逐帧求值，能力现成 |
| 参数面 | 种子点数量与位置：建议**写死常量**（比如 3 点：触发点 + 两个按视口比例算出的点），不开选项——一开就是第 7 个局部参数 |
| 待验点 | 触发点之外那几个点的坐标规则要不要跟着视口尺寸走；末帧所有层并集必须盖满四角 |
| 状态 | 待做 |

补的是 §2 表里"多中心"这个真空缺。

### P1-4 `BLINDS` 交错开合

| 项 | 内容 |
| --- | --- |
| 观感 | 奇数叶与偶数叶分两批错开半个周期揭开，比现在的同步百叶窗更有层次 |
| 机制增量 | **无**（两层条带 + 错时） |
| 实现要点 | 奇偶批各一层条带渐变，周期取 `2 × slatWidth`，两层进度各错开 `slatWidth`，`add` 并集 |
| 参数面 | 建议做成新类型 `BLINDS_ALT` 而不是给 BLINDS 加选项（加选项就是第 7 个局部参数） |
| 待验点 | 与 `QR_GRID` 的观感距离够不够远——两者都是"分批生长"，可能做出来发现像了 |
| 状态 | 待做 |

**这条是候选池里最可能被"观感重复"否掉的**，做之前建议先拿 P0 两个垫高信心再排。

### P2-1 通用反向 `revert`

| 项 | 内容 |
| --- | --- |
| 观感 | 让 10 个中心扩散类都能"收起"（旧主题向外让开、新主题从四周显出），收益 = 观感翻倍 |
| 机制增量 | 小（蒙版从终尺寸缩到 0，`pinCenterGeometry` 已有两种钉扎方向） |
| 卡点 | **`CIRCLE_REVERT` 会同时存在"类型"和"选项"两种写法**。要先定策略：保留该类型不动 + 选项明确写着"不影响它"，还是把它标 deprecated 走一轮破坏性移除 |
| 参数面 | 一个布尔选项，但它作用于 10 个类型，README 的 options 表要加一列说明适用范围 |
| 状态 | 待做（**先要一次决策，不要直接开工**） |

### P2-2 `LOGO_MASK` 自定义轮廓

| 项 | 内容 |
| --- | --- |
| 观感 | 新主题以用户给的图形轮廓（logo、心形、任意 path）揭开 |
| 机制增量 | 无（形状族本来就是 SVG data-URI 蒙版，换成用户输入） |
| 卡点 | **把用户输入引进蒙版字符串 = 真实 XSS 面**。data-URI 里的 `on*` 属性、`<script>`、`href` / `xlink:href` 都是能出事的。必须先定白名单：只接受 `<path d>` / `<polygon points>` / `viewBox`，拒绝其它一切，且要有对应单测 |
| 附带问题 | 终尺寸仍要按"内切半径盖住视口最远角"算，而用户轮廓的内切半径未知 → 要么用 `getBBox` 量（需要 DOM）、要么退化成外接方框（观感偏大） |
| 状态 | 待做（**单独排一期**，消毒规则值得一次专门讨论，别顺手做） |

---

## 4. 明确不做（防止重复讨论）

| 候选 | 否掉的理由 |
| --- | --- |
| 3D 翻页 / 折叠 | 要放弃 mask、改动画 `::view-transition-group` 的 transform，直接破约束 1；且 `masks.ts` 已记录 Safari 忽略伪元素上的 WAAPI 与 clip-path |
| 文字 / 描边书写揭示 | 越界（不是"主题揭开"而是内容动画），且蒙版尺寸随视口变化会失真 |
| `DISSOLVE` 沙化溶解 | 撞约束 2：噪声天然留镂空，末帧洗不干净。要绕开得做"实心段 ∪ (噪声 ∩ 前缘带)"三层 + per-layer composite，比涟漪高一档以上，且 per-layer composite 的支持面比 QR_GRID 现在用的单层 intersect 更窄 |
| `PIXELATE` 随机马赛克 | CSS 表达不了"随机顺序"，为每格准备属性的话图层数与属性数一起爆 |
| 真·水面折射位移 | 撞约束 3：转场期只有伪元素能动，`feDisplacementMap` 位移做不了（`RIPPLE` 只能是"环形蒙版揭示"） |
| 边角擦入 / 对角展开 | 观感接近 `SCAN` 的参数变体，该作为 `direction` 的新取值或 `origin` 选项，不占类型名额 |
| 继续加多边形（八边形、十字、心形…） | 形状族已 7 个饱和，边际观感收益接近零 |

---

## 5. 撤回约定

按"做完一个验证一个，不行就整个撤回"的节奏，每个候选的改动面是可枚举的。撤回某一条时按这个清单删：

1. `packages/core/src/types.ts` —— 类型值、`ThemeAnimationType` 里的注释、若有新选项则含 options 字段 / `ResolvedAnimationOptions` / 默认值常量 / 校验函数
2. `packages/core/src/masks.ts` —— spec 函数与常量、守卫函数、若有新机制则含 `RevealMaskSpec` 字段
3. `packages/core/src/orchestrate.ts` —— 分发分支与 import
4. `packages/core/src/index.ts` —— 导出名
5. 四处测试 —— `types.test.ts`（类型数量与 options 快照）、`masks.test.ts`、`orchestrate.dom.test.ts`、`index.test.ts`（导出面清单）
6. `apps/docs` —— 画廊卡与图标、hero / features / `site.ts` 的数字与措辞、`assets/screen.jpg` 重出
7. 四个 playground —— 类型清单、控件、引导文案数字
8. `.changeset/*.md` —— 删掉对应那条
9. `docs/requirements.md` —— 若已写修订记录，改为"曾尝试、已撤回"一句（不删记录，留判断痕迹）
10. 本文件 —— 状态改成 `已撤回` 并保留理由

**顺序建议**：先只落 core + 画廊（第 1–6 项的前半）拿观感结论，通过后再补 README / 需求文档 / playground / changeset / 门面截图。三轮下来的经验是门面那批同步占大头，提前撤要便宜得多。

---

## 6. 每加一个类型的固定成本

一个类型从落地到文档齐平，硬编码数字要改 5 处以上：README 首段 + 特性清单 + options 表、文档站 hero 两处、features、`site.ts` SEO、四个 playground 的"N 个按钮"，外加**门面截图必须重出**（hero 文案一改就得重拍）。

两个可选的减负方向，需要单独拍板：

- README 特性清单不再写死数字，改成只列家族（"圆形 / 形状 / 条带格子 / 环带 / 角度"），数字只留在文档站 hero
- 攒 2~3 个类型一起同步一轮，摊薄成本（代价是一致性窗口变长）
